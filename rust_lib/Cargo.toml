[package]
name = "rust_lib"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_lib"
crate-type = ["cdylib"]  # Python拡張モジュールとしてビルドするために必要

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }

[dev-dependencies]
tempfile = "3"